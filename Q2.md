//q2
//vnte OUL quenes vor the following operations on the Hotels tabli
//Insert two hotels
//Update the number of available rooms
//Delete a hotel using hotel_id
//Calculate the total number of rooms available (SUM)
//Calculate the average rooms available (AVG)

INSERT INTO Hotels (hotel_id, hotel_name, city, rooms_available)
VALUES (1, 'Taj Palace', 'Mumbai', 120);

INSERT INTO Hotels (hotel_id, hotel_name, city, rooms_available)
VALUES (2, 'The Oberoi', 'Delhi', 95);

UPDATE Hotels
SET rooms_available = 150
WHERE hotel_id = 1;

DELETE FROM Hotels
WHERE hotel_id = 2;

SELECT SUM(rooms_available) AS total_rooms
FROM Hotels;


SELECT AVG(rooms_available) AS average_rooms
FROM Hotels;
