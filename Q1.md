//Write SOL queries to create a tahip with:
//hotel_name VARCHAR(60) (Unique)
//city VARCHAR(40)
//rooms_available INT (Not Null)
//Add constraints:
//Primary key on hotel_id
//Unique key on hotel_name
//rooms_available cannot be NULL

CREATE TABLE Hotels (
    hotel_id INT PRIMARY KEY,
    hotel_name VARCHAR(60) UNIQUE,
    city VARCHAR(40),
    rooms_available INT NOT NULL
);
